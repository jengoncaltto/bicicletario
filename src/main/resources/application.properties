# Porta do servidor
server.port=80

# Configuracoes do banco de dados PostgreSQL
#spring.datasource.url=jdbc:postgresql://localhost:5432/aluguel
#spring.datasource.username=seu_usuario
#spring.datasource.password=

# Dialeto e comportamento do Hibernate (JPA)
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update

# Mostra as queries SQL no console (util em dev)
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# (Opcional) Define o timezone padrao para evitar divergencias de data/hora
#spring.jpa.properties.hibernate.jdbc.time_zone=UTC
